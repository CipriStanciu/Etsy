# Fragrance Bot runtime dependencies.
#
# The recipe engine (fragbot/) is standard-library ONLY.
# The listing image generator (fragbot/imagesgen/) needs Pillow — a free,
# open-source imaging library (no paid services, no network at render time).
# The recipe-card PDF generator (fragbot/pdfgen/) needs ReportLab — a free,
# BSD-licensed PDF library (no network at render time, vendored free fonts).
# pypdf (BSD) is used by the PDF verifier (verify_pdfs.py / `pdfgen verify`).
Pillow>=10.0
reportlab>=4.0
pypdf>=4.0
# Optional — only the direct-SQL backend of the Supabase storage layer
# (fragbot/db.py PostgresStore) needs psycopg; the REST backend is stdlib-only.
#   pip install 'psycopg[binary]'
# psycopg[binary]>=3.1
# The Etsy API client (fragbot/etsy), the daily posting pipeline
# (scripts/daily_post.py) and the local verification mock (scripts/mock_etsy.py)
# are 100% standard-library (urllib) — no extra dependency, no paid services.
# The optional Resend notification email also uses urllib directly.
